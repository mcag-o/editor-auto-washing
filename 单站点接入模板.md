# 单站点接入模板

本文档是 `爬虫开发和接入.md` 的配套执行模板。

定位：

- 前者负责定义规范
- 本文负责提供可直接复制、填写、执行、验收的单平台接入工单模板

目标：让后续开发人员拿到一个站点后，不需要自己重新拆阶段，就能按统一格式完成从 `placeholder` 到真实实现的接入。

---

## 1. 使用说明

推荐使用方式：

1. 复制本文档作为某个平台的接入工单
2. 将所有占位符替换为真实值
3. 按阶段逐项勾选
4. 每完成一个阶段，就补测试并提交
5. 完成后将结果同步回主文档与平台状态说明

建议不要直接在主文档里一边想一边改；先按模板收口，后续审查会更容易。

---

## 2. 占位符填写规范

本节用于消除模板使用过程中的歧义。

### 2.1 `<平台中文名>`

- 类型：`string`
- 必填：是
- 示例：
  - `知乎热榜`
  - `掘金`
  - `雪球`

要求：

- 应与配置中的 `display_name` 保持一致
- 建议使用团队内已有中文命名，不要同一平台出现多个叫法

### 2.2 `<source-id>`

- 类型：`string`
- 必填：是
- 格式：小写英文，允许下划线
- 示例：
  - `zhihu`
  - `juejin`
  - `sina_finance`

要求：

- 必须与 `CollectorSourceDef`、`config/config.json`、plugin `SourceID()` 完全一致

### 2.3 `<implementation-reference>`

- 类型：`string`
- 必填：强烈建议
- 示例：
  - `source:zhihu`
  - `source:juejin`
  - `collector/plugin/sources/zhihu.go`

要求：

- 应填写当前仓库内可长期保留的实现参考

### 2.4 `<目标状态>`

- 类型：枚举 `string`
- 合法值：
  - `implemented-equivalent`
  - `implemented-partial`

说明：

- `implemented-equivalent`：已达到目标能力范围
- `implemented-partial`：已有真实实现，但能力尚未补齐

---

## 3. 单平台接入总目标

某站点接入完成后，应满足：

- 进入统一 collector runtime
- 具备至少 hotlist 能力
- 如业务需要，具备 detail/article 能力
- 使用统一配置、统一 auth、统一 HTTP、统一 retry 路径
- 有 fixture、有测试、有 health、有可定位错误信息

---

## 4. 阶段总览

推荐严格按以下 6 个阶段推进：

1. 平台调研与能力盘点
2. 配置与占位信息整理
3. hotlist 实现
4. detail/article 实现（如需要）
5. runtime 接入与统一链路验证
6. 状态收口与能力确认

不要跳阶段直接先写插件代码。

---

## 5. 接入工单模板（标准版）

下面这部分是推荐的正式接入工单模板，可直接复制填写。

```md
# <平台中文名> 接入工单

## A. 平台元信息

- 平台名称：`<平台中文名>`
- source id：`<source-id>`
- 实现参考：`<implementation-reference>`
- 平台类型：`json-api / html / hybrid`
- 是否需要认证：`none / cookie / header / token / other`
- 是否支持 detail：`yes / no / unknown`
- 当前 Go 状态：`placeholder / partial / implemented`
- 目标状态：`implemented-equivalent / implemented-partial`
- 优先级：`high / medium / low`

## B. 输入信息表

### 1. hotlist 来源
- URL/Path：`<必填>`
- 方法：`GET / POST / other`
- 是否依赖特殊 query：`yes / no`
- query 示例：`<可选>`

### 2. detail 来源
- 是否需要：`yes / no`
- URL/Path：`<如适用则必填>`
- 方法：`GET / POST / other`
- 来源类型：`api / html / hybrid`

### 3. 认证需求
- auth 模式：`none / cookie / header / token`
- secret ref：`env.<XXX>`
- 是否需要特殊 Referer：`yes / no`
- 是否需要特殊 UA：`yes / no`

### 4. 运行时需求
- `http_client`：`<profile-id>`
- `retry_policy`：`<profile-id>`
- `auth_profile`：`<profile-id>`
- `timeout_ms`：`<number>`
- `hotlist_limit`：`<number>`

## C. 输出能力表

### hotlist 输出字段
- [ ] `SourceID`
- [ ] `ExternalID`
- [ ] `CanonicalURL`
- [ ] `Title`
- [ ] `Summary`
- [ ] `Rank`
- [ ] `RawJSON`
- [ ] `Metadata`

### detail 输出字段（如需要）
- [ ] `SourceID`
- [ ] `ExternalID`
- [ ] `CanonicalURL`
- [ ] `Body`
- [ ] `ContentType`
- [ ] `Metadata`

## D. 阶段一：平台调研

- [ ] 确认 hotlist 来源
- [ ] 确认 detail 来源
- [ ] 确认 auth 需求
- [ ] 确认 headers/query/签名要求
- [ ] 确认字段映射
- [ ] 确认失败模式
- [ ] 确认 fixture 是否可复用

### 本阶段结论
- hotlist 来源：`<填写>`
- detail 来源：`<填写>`
- auth 需求：`<填写>`
- 风险点：`<填写>`

## E. 阶段二：配置与占位整理

- [ ] `collector_config.go` 已补齐该平台 `CollectorSourceDef`
- [ ] `display_name` 已填写
- [ ] `aliases` 已填写
- [ ] `source_type` 已填写
- [ ] `source_url` 已填写
- [ ] `status` 已填写
- [ ] `goal` 已填写
- [ ] `todo` 已填写
- [ ] `notes` 已填写
- [ ] `implementation_reference` 已填写为当前实现参考
- [ ] `supports_article` 已填写
- [ ] `auth_profile` 已填写
- [ ] `config/config.json` 已补齐 source 配置
- [ ] `http_client` 已配置
- [ ] `retry_policy` 已配置
- [ ] `auth_profile` 已配置
- [ ] `timeout_ms` 已配置
- [ ] `hotlist_limit` 已配置

### 本阶段产出物
- source 定义状态：`<placeholder / partial / implemented>`
- 中文目标说明：`<填写>`

## F. 阶段三：hotlist 实现

- [ ] 插件文件已创建：`collector/plugin/sources/<source-id>.go`
- [ ] `SourceID()` 已实现
- [ ] `DisplayName()` 已实现
- [ ] `Aliases()` 已实现
- [ ] `FetchHotlist()` 已实现
- [ ] `NormalizeHotEntry()` 已实现
- [ ] `HealthCheck()` 已实现
- [ ] `Capabilities()` 已实现
- [ ] `Descriptor()` 已实现
- [ ] 如需 runtime client 注入，`WithHTTPClient()` / `HTTPClient()` 已实现

### hotlist fixture / test
- [ ] 正常响应 fixture 已添加
- [ ] 异常响应 fixture 已添加
- [ ] hotlist 成功测试已添加
- [ ] hotlist 失败测试已添加
- [ ] health 成功测试已添加
- [ ] health 失败测试已添加

### 本阶段验收
- [ ] 该平台能够进入 run 主流程
- [ ] 能生成 collector entries
- [ ] source health 可用

## G. 阶段四：detail/article 实现（如需要）

- [ ] 已确认 detail 来源
- [ ] `FetchArticle()` 已实现
- [ ] `NormalizeArticle()` 已实现
- [ ] detail fixture 已添加
- [ ] detail 成功测试已添加
- [ ] detail 失败测试已添加

### 本阶段验收
- [ ] 可从 entry 获取 article
- [ ] 可得到标准化正文
- [ ] 如需要，已能进入 bridge 路径

## H. 阶段五：runtime 接入与统一链路验证

- [ ] `PolicyResolver` 解析通过
- [ ] `AuthFactory` 处理通过
- [ ] `HTTPFactory` 构造通过
- [ ] `runtime_plugin_adapter` 注入通过
- [ ] `RunService` 真实 runtime path 测试通过
- [ ] `SourceRegistryService` runtime-aware health 测试通过
- [ ] source sync metadata 持久化测试通过

### 本阶段验收
- [ ] 不再依赖插件内部 secret 解析
- [ ] 不再依赖插件内部 retry/auth fallback
- [ ] 真实运行时已走统一配置/认证/HTTP/重试路径

## I. 阶段六：状态收口与替代确认

- [ ] placeholder 已替换
- [ ] `status` 已更新
- [ ] `goal/todo/notes` 已更新
- [ ] 相关文档已同步
- [ ] 已明确该平台当前状态

### 最终状态
- 最终状态：`placeholder / partial / implemented`
- 替代结论：`implemented-equivalent / implemented-partial / not-ready`
- 说明：`<填写>`
```

---

## 6. 各阶段详细说明

### 6.1 阶段一：平台调研

目的：先弄清目标平台的真实能力与约束。

必须回答：

- hotlist 从哪里来
- detail 从哪里来
- auth 怎么工作
- 当前参考实现或现网请求有哪些 header/query/selector 规则
- 常见失败模式是什么

边界条件：

- 如果参考实现本身不稳定，要如实记录，不要直接继承问题
- 如果 detail 是否需要尚不明确，状态应写 `unknown`，不要强行假设

### 6.2 阶段二：配置整理

目的：先让平台进入统一体系。

设计意图：

- 让后续开发者一眼看到该平台当前状态、目标和剩余工作
- 让该平台先进入 source catalog，而不是等代码写完才补配置

### 6.3 阶段三：hotlist 实现

目的：先获得最小可用真实能力。

边界条件：

- 如果 API 结构复杂，至少先保证关键字段映射完整
- 如果 HTML 易变，优先增加 fixture，而不是先追求“看起来解析成功”

### 6.4 阶段四：detail 实现

目的：补足需要 bridge / 正文处理的平台能力。

判断标准：

- 如果该平台只是热点发现用途，可以不强行做 detail
- 如果该平台后续要转成文章工作区，则通常必须做 detail

### 6.5 阶段五：runtime 接入

目的：确认该平台不是“插件能跑”，而是“系统已接入”。

这是整个接入过程中最容易被忽略但最重要的一步。

只要该平台没有经过：

- resolver
- auth factory
- HTTP factory
- runtime adapter
- service path

就不能认为接入完成。

### 6.6 阶段六：状态收口

目的：避免平台状态长期停留在模糊状态。

必须最终明确：

- 它还是 placeholder 吗
- 它只是 partial 吗
- 它是否已真正达到 implemented

---

## 7. 示例：以 `zhihu` 为例的填写样式

下面给出一个简化示例，帮助开发者理解字段应该如何填写。

```md
# 知乎热榜 接入工单

- 平台名称：`知乎热榜`
- source id：`zhihu`
- 实现参考：`source:zhihu`
- 平台类型：`json-api`
- 是否需要认证：`none`
- 是否支持 detail：`yes`
- 当前 Go 状态：`placeholder`
- 目标状态：`implemented-equivalent`

## 阶段一：调研结论
- hotlist 来源：`https://www.zhihu.com/api/v3/explore/guest/feeds`
- detail 来源：`question/answer 页面或配套 detail 接口`
- auth 需求：`none`
- 风险点：`字段层级复杂，需先稳定映射 raw payload`

## 阶段二：配置
- `http_client`: `default_api_client`
- `retry_policy`: `default_api`
- `auth_profile`: `none`
- `timeout_ms`: `12000`
- `hotlist_limit`: `30`

## 当前判断
- hotlist 先做
- detail 必须做
- 最终目标：`implemented-equivalent`
```

---

## 8. 常见错误示例

### 错误示例一：source id 不一致

错误写法：

- `collector_config.go` 写 `zhihu`
- 插件 `SourceID()` 返回 `zhi_hu`

后果：

- source sync、registry、run path 会出现不可预期问题

### 错误示例二：有 auth 需求但未配 profile

错误写法：

- 在文档里写需要 cookie
- 但 `config/config.json` 没配 `auth_profile`

后果：

- 运行时无法统一接入 auth factory

### 错误示例三：已实现 hotlist 就标记 implemented

错误写法：

- detail 还没做
- auth 还没做
- runtime 接入还没做
- 却直接标记 `implemented`

正确做法：

- 标记 `partial`

### 错误示例四：TODO 太空泛

错误：

- `继续完善`

正确：

- `补 detail fixture`
- `补 auth 失败 health 测试`
- `补 source sync metadata 持久化测试`

---

## 9. 推荐提交粒度

建议按阶段提交，不要把所有工作压在一个巨型提交里。

推荐粒度：

1. `feat: add <source-id> collector config scaffold`
2. `feat: implement <source-id> hotlist plugin`
3. `feat: add <source-id> article fetch support`
4. `test: add <source-id> fixtures and runtime coverage`
5. `docs: update <source-id> implementation status`

---

## 10. 最终提醒

单站点接入最常见的三类问题：

1. 只写抓取逻辑，不接统一 runtime
2. 只写成功路径，不补 fixture 和失败测试
3. 把 secret、retry、timeout、auth 又重新硬编码回插件内部

只要按本文档执行，这个平台就不是一个“能跑一下的站点脚本”，而是一个能进入当前 collector 主系统长期维护的正式 source 实现。
