# 爬虫开发和接入

本文档是当前仓库 collector 的站点开发与接入规范。

它不是“如何快速写一个抓取脚本”的教程，而是“如何把一个站点稳定、可维护、可测试地接入到 Go 主系统”的标准说明。后续所有平台接入、占位替换、功能补齐、测试收口，都应优先遵守本文档。

配套模板：`单站点接入模板.md`

---

## 1. 适用范围

本文档适用于以下工作：

- 为某个平台补齐当前 collector 实现
- 将当前 `placeholder` 平台替换为真实插件实现
- 为已有平台补充 hotlist / detail / auth / health / fixture / test 能力
- 将独立抓取逻辑接入统一 runtime、配置、认证、重试与调度链路

本文档不适用于：

- 只做一次性实验抓取
- 临时调试脚本
- 与当前仓库 collector 无关的独立爬虫项目

---

## 2. 文档目标

本文档解决四类问题：

1. 单站点爬虫做到什么程度才算“合格接入”
2. 每个平台该放在哪里、实现哪些接口、接入哪些运行时能力
3. 配置、认证、HTTP、重试、health、fixture、测试分别怎么接
4. 如何判断一个站点已经达到当前系统的接入标准

---

## 3. 核心原则

### 3.1 不是独立脚本，而是统一运行时插件

后续开发的站点，不能只停留在：

- 一个独立 Go 文件
- 一个命令行实验程序
- 一个能打印 JSON 的临时脚本

必须最终进入统一 collector runtime：

- 统一 `source id`
- 统一插件接口
- 统一配置模型
- 统一 auth / HTTP / retry / timeout 路径
- 统一 source sync 持久化
- 统一 `run / health / scheduler` 执行链路

设计意图：

- 避免后续出现“功能上能跑，但运维层面不可控”的孤立实现
- 避免形成新的平行采集系统
- 让每个平台都能共享同一套配置、认证、重试、测试和审计机制

### 3.2 优先复用运行时能力，不要在站点内部重复发明

已有统一能力如下：

- HTTP client 构造：`collector/runtime/http_factory.go`
- auth 注入：`collector/runtime/auth_factory.go`
- source 策略解析：`collector/runtime/policy_resolver.go`
- retry/backoff/classifier：`collector/httpclient/retry.go`
- runtime 注入适配：`collector/service/runtime_plugin_adapter.go`

站点代码应主要关注：

- 请求路径与请求参数
- JSON 字段映射或 HTML 选择器
- 标准化逻辑
- 站点特有边界条件
- 站点特有错误语义

不要优先在插件内部重复实现：

- secret 解析
- 重试循环
- timeout 策略
- auth header 拼装
- 独立 HTTP client 管理

### 3.3 先进入体系，再补能力

一个尚未迁完的平台，建议按顺序推进：

1. 先补 source 配置和占位说明
2. 再补真实插件骨架
3. 再补 hotlist
4. 再补 detail/article
5. 再补 runtime 接入
6. 再补 fixture 与回归测试
7. 最后再收口状态与文档

不要反过来先写很多代码，再回头拼接运行时。

原因：

- 先接入体系可以尽早暴露配置、认证、运行时边界问题
- 先写脚本后接入，通常会把临时逻辑固化进插件内部，后面更难清理

---

## 4. 关键术语表

### 4.1 Source

- 类型：业务概念
- 含义：一个采集平台的统一标识与配置单元，例如 `zhihu`、`weibo`
- 作用：连接配置、registry、plugin、run、health、scheduler、storage

### 4.2 Placeholder

- 类型：状态概念
- 含义：平台已经进入统一配置目录与 runtime 体系，但真实抓取能力尚未完成
- 特征：有 source 定义、有 goal/todo、有实现参考，但仍缺真实实现

### 4.3 Runtime-aware path

- 类型：运行路径概念
- 含义：source 执行时通过 resolver + auth factory + HTTP factory + runtime adapter 的统一路径运行
- 作用：保证 auth、timeout、retry、headers、base URL 等由统一策略控制

### 4.4 Hotlist

- 类型：能力概念
- 含义：列表/热榜/热门主题抓取能力
- 输出：`plugin.HotEntry`

### 4.5 Detail / Article

- 类型：能力概念
- 含义：从 hotlist entry 深入抓取正文或详情页内容的能力
- 输出：`plugin.RawArticle` / `plugin.NormalizedArticle`

### 4.6 Fixture

- 类型：测试资产
- 含义：站点响应样本，用于 regression test 与结构变更回归
- 常见形式：JSON、HTML、auth failure 响应、detail page 响应

---

## 5. 当前统一接入点

后续开发最常用文件如下。

### 5.1 配置层

- `infra/config/collector_config.go`
- `config/config.json`

用途：

- 定义 source
- 定义 profile
- 定义 goal / todo / implementation reference
- 定义运行时策略入口

### 5.2 插件层

- `collector/plugin/interface.go`
- `collector/plugin/sources/`

用途：

- 站点真实抓取逻辑
- 标准化逻辑
- health 与 capabilities 表达

### 5.3 运行时层

- `collector/runtime/policy_resolver.go`
- `collector/runtime/auth_factory.go`
- `collector/runtime/http_factory.go`
- `collector/service/runtime_plugin_adapter.go`

用途：

- 将 source config 解析成运行时有效配置
- 将 secret ref 变成 auth injector
- 将 source-scoped 配置变成真实 HTTP client
- 将 client 注入到插件运行路径

### 5.4 服务与装配层

- `collector/service/registry.go`
- `collector/service/run_service.go`
- `collector/service/source_registry_service.go`
- `service/collector_runtime.go`

用途：

- source sync
- source health
- run 执行
- runtime 装配与 scheduler 使用

---

## 6. 数据类型与占位符规范

本节用于彻底消除模板与配置中的歧义。

### 6.1 `source-id`

- 类型：`string`
- 必填：是
- 默认值：无
- 格式约束：
  - 小写英文
  - 可包含下划线 `_`
  - 不建议包含空格、中文、大写字母
- 合法示例：
  - `zhihu`
  - `sina_finance`
  - `jinritoutiao`
- 非法示例：
  - `ZhiHu`
  - `知乎`
  - `zhihu hot`

业务要求：

- 必须与 `collector_config.go`、`config/config.json`、registry、plugin `SourceID()` 完全一致
- 一旦公开使用，不应轻易改名

### 6.2 `platform type`

- 类型：枚举 `string`
- 必填：是
- 合法值：
  - `json-api`
  - `html`
  - `hybrid`
- 默认值：无

说明：

- `json-api`：主要通过 API 拉取
- `html`：主要通过页面解析
- `hybrid`：hotlist 和 detail 来源不同，或混合使用 API + HTML

### 6.3 `status`

- 类型：枚举 `string`
- 必填：是
- 合法值：
  - `placeholder`
  - `partial`
  - `implemented`
- 默认值：`placeholder`

语义：

- `placeholder`：只有占位与说明，无稳定真实实现
- `partial`：已有真实实现，但只覆盖部分能力，例如 hotlist-only
- `implemented`：已覆盖当前目标能力，进入稳定维护阶段

### 6.4 `auth mode`

- 类型：枚举 `string`
- 必填：视平台而定
- 合法值：
  - `none`
  - `cookie`
  - `header`
- 默认值：来自 `auth_profile` 或 `defaults`

注意：

- 在当前配置模型中，推荐优先使用 `auth_profile`
- 不应只写 `auth_mode` 而没有明确 profile 来源

### 6.5 `implementation_reference`

- 类型：`string`
- 必填：强烈建议
- 默认值：空字符串
- 格式约束：使用当前项目的稳定引用标识，推荐 `source:<source-id>`
- 合法示例：
  - `source:zhihu`
  - `source:juejin`

作用：

- 帮助后续开发者快速定位当前 source 定义对应的实现参考
- 便于审计平台实现状态与后续工作

### 6.6 `headers`

- 类型：`map[string]string`
- 必填：否
- 默认值：空 map

示例：

```json
{
  "Accept": "application/json",
  "Referer": "https://www.zhihu.com/",
  "X-Requested-With": "XMLHttpRequest"
}
```

边界条件：

- 空 map 表示不额外覆盖 header
- source headers 会覆盖 profile headers 中同名字段

### 6.7 `todo`

- 类型：`[]string`
- 必填：是
- 默认值：空切片

要求：

- 每一项必须是具体开发动作，不要写空泛描述

推荐写法：

- `实现热榜字段映射`
- `补 detail fixture`
- `补 auth 失败测试`

不推荐写法：

- `继续优化`
- `后续完善`
- `需要开发`

---

## 7. 单站点完成标准

### 7.1 最低可接入标准

- 在 `collector_config.go` 中存在 source 定义
- 在 `config/config.json` 中有对应 source 配置
- 在 `collector/plugin/sources/` 中存在真实插件，不再是 placeholder
- 能进入 registry / source sync / run / health 主流程
- 至少有最基本测试

### 7.2 实用标准

- 支持 hotlist/list 抓取
- 输出标准化 `HotEntry`
- 支持 runtime config 注入后的运行
- 使用统一 auth / HTTP / retry 路径
- 对常见失败有清晰错误信息

### 7.3 等价替代标准

- 已覆盖当前目标抓取能力
- 若目标范围包含 detail，则 Go 版也支持 detail
- 若平台依赖 cookie/header/token，则 Go 版也支持通过配置接入
- 有 fixture 驱动回归测试
- health 与失败分类能满足运维定位

---

## 8. 开发流程规范

### 第一步：平台调研

先明确：

- hotlist 来源是什么
- detail 来源是什么
- 是否需要认证
- 当前可参考实现或现网请求的 headers/query/selector/signature 是什么
- 是否有 fixture 可复用

本阶段不要急着写正式插件。

### 第二步：配置进入体系

必须先在：

- `infra/config/collector_config.go`
- `config/config.json`

中补齐 source 定义、profile 引用、中文说明和实现参考。

设计意图：

- 让平台先“进入体系”
- 让后续开发者先知道要做什么，再开始写实现

### 第三步：实现 hotlist

必须先把最小可用的热榜路径跑通：

- `FetchHotlist()`
- `NormalizeHotEntry()`
- `HealthCheck()`
- 对应 fixture 与测试

### 第四步：实现 detail/article（如需要）

如果平台后续需要 bridge 或正文能力，再继续做：

- `FetchArticle()`
- `NormalizeArticle()`

### 第五步：接入运行时

必须验证该插件真实走的是：

- `PolicyResolver`
- `AuthFactory`
- `HTTPFactory`
- `runtime_plugin_adapter`
- `RunService` / `SourceRegistryService`

如果插件还只能在自己内部特殊构造 client 才能跑，说明接入未完成。

### 第六步：收口状态

完成后更新：

- `status`
- `goal`
- `todo`
- `notes`

把平台从 `placeholder` 收口到 `partial` 或 `implemented`。

---

## 9. 插件接口契约

### 9.1 必须实现的核心接口

参考：`collector/plugin/interface.go`

必须实现：

- `SourceID() string`
- `DisplayName() string`
- `Aliases() []string`
- `FetchHotlist(ctx, req)`
- `FetchArticle(ctx, req)`
- `NormalizeHotEntry(raw)`
- `NormalizeArticle(raw)`
- `HealthCheck(ctx)`
- `Capabilities()`

如需进入统一运行时 HTTP 注入路径，还应支持：

- `WithSourceConfig(source domain.CollectorSource)`
- `WithHTTPClient(client *httpclient.Client)`
- `HTTPClient() *httpclient.Client`

### 9.2 `FetchHotlist()` 规范

目的：抓取该 source 的热榜/列表。

输入：

- `ctx`：请求上下文，用于超时与取消
- `req.Limit`：期望返回的最大条数
- `req.Headers`：调用方附加请求头

输出：

- `[]plugin.HotEntry`
- 错误 `error`

边界条件：

- 空列表必须安全返回，不应 panic
- 结构变更应返回可定位错误，不应静默吞掉
- 应尽量保留 `RawJSON` 或 raw metadata

### 9.3 `FetchArticle()` 规范

目的：拉取详情页或正文。

如果支持：

- 必须明确 detail 来源
- 返回 `plugin.RawArticle`
- 提供 `ContentType`

如果不支持：

- 明确返回 `plugin.ErrArticleFetchNotSupported(...)`

### 9.4 `NormalizeHotEntry()` / `NormalizeArticle()` 规范

目的：字段标准化。

原则：

- 不做额外 I/O
- 不做跨 source 逻辑
- 错误信息要能定位字段与来源

### 9.5 `HealthCheck()` 规范

目的：给出最小真实可用性探测。

返回结构：

- `SourceID`
- `OK`
- `Code`
- `CheckedAt`
- `Message`

推荐 `Code`：

- `healthy`
- `unavailable`
- `auth_missing`
- `auth_expired`

注意：

- 不要把所有错误都归成同一种状态
- 但也不要在插件内部再造一套认证框架，优先消费 runtime 注入结果

### 9.6 `Descriptor()` 规范

这是 source sync 持久化的重要描述面。

建议尽量提供：

- `Enabled`
- `ScheduleEnabled`
- `IntervalMinutes`
- `TimeoutMS`
- `HotlistLimit`
- `DetailFetchEnabled`
- `Concurrency`
- `AuthMode`
- `Headers`
- `RetryPolicy`
- `Options`
- `Metadata`

设计意图：

- descriptor 负责表达“插件能力”和“最小默认描述”
- 最终持久化时，系统会把 resolved runtime metadata 与 descriptor 合并

---

## 10. 配置接入规范

### 10.1 `CollectorSourceDef` 最低要求

每个平台至少补齐：

- `DisplayName`
- `SourceType`
- `SourceURL`
- `HTTPClient`
- `RetryPolicy`
- `AuthProfile`
- `TimeoutMS`
- `Headers`
- `Status`
- `Goal`
- `Todo`

### 10.2 profile 使用优先级

推荐优先顺序：

1. `defaults`
2. 命名 `http_clients`
3. 命名 `retry_policies`
4. 命名 `auth_profiles`
5. source 级少量 override

不要反过来把所有东西重新塞回插件构造函数里。

### 10.3 secret 引用规范

敏感信息必须通过 ref 表达，不能写死在插件内部。

当前推荐：

- `env.WEIBO_COOKIE`
- `env.XUEQIU_COOKIE`
- `env.GITHUB_TOKEN`

当前 collector runtime 已支持 `env.*` secret resolver。

---

## 11. 认证接入规范

### 11.1 禁止在插件内部解析 secret

禁止再新增以下模式：

- `os.Getenv(...)` 写在 plugin 里
- plugin 自己决定默认 secret ref
- plugin 自己拼装 cookie/token/header

统一做法：

- source config 提供 ref
- resolver 解析 auth profile
- auth factory 构造 injector
- runtime adapter 将 injector 作用到 client
- 插件只消费注入后的 client

### 11.2 当前已实现的真实路径

当前系统已经打通的 auth 外置路径，至少可作为标准参考：

- Weibo cookie auth
- runtime-aware service path 中的 secret ref -> injector -> client 注入链路

后续新增平台，应尽量遵循这一路径，而不是重新发明内部认证逻辑。

---

## 12. HTTP 与重试接入规范

### 12.1 不要绕开 `HTTPFactory`

如果插件真实运行时没有走 `HTTPFactory` 构造出来的 client，就不能认为它已经完成接入。

### 12.2 不要在插件里重复实现 retry loop

应优先复用：

- `RetryClassifier`
- `ExponentialBackoff`
- `MaxWait`
- `Jitter`

设计意图：

- 让 retry 策略能被统一配置和审计
- 避免每个平台出现不同风格、不可控的 sleep/retry 逻辑

### 12.3 header 合并规则

推荐语义：

- HTTP profile 提供默认 headers
- source headers 提供覆盖
- auth factory 注入认证头
- 插件不要覆盖掉 runtime 已经决定的结果

---

## 13. Fixture 与测试规范

### 13.1 每个平台最低测试要求

- hotlist 成功测试
- hotlist 失败测试
- health 成功测试
- health 失败测试
- 如支持 detail，则补 detail 成功/失败测试

### 13.2 每个平台最低 fixture 要求

- 正常响应 fixture
- 空响应或结构异常 fixture
- auth 失败 fixture（如适用）
- detail fixture（如适用）

### 13.3 统一链路测试要求

除了插件测试，还应有接入层测试：

- resolver 测试
- auth factory 测试
- HTTP factory 测试
- run service 测试
- source health 测试
- source sync metadata 持久化测试

---

## 14. 状态定义与验收标准

### 14.1 状态值定义

#### `placeholder`

- 无真实稳定实现
- 只有配置占位、goal、todo、implementation reference

#### `partial`

- 有真实实现
- 但只覆盖部分能力
- 典型例子：仅 hotlist，没有 detail

#### `implemented`

- 已覆盖当前目标能力
- 进入稳定维护阶段

### 14.2 何时可宣布“已接入当前系统”

建议至少满足：

- hotlist 完成
- detail（如需要）完成
- auth 完成
- runtime 接入完成
- fixture 完成
- 回归测试完成
- 不再依赖仓库外或兼容性临时脚本

---

## 15. 常见错误与反例

### 反例一：只写抓取代码，不接 runtime

错误做法：

- 平台能抓数据
- 但没接 resolver/auth/http factory

后果：

- 配置不可控
- 认证不可统一管理
- 测试与实际运行路径不一致

### 反例二：在插件里直接解析 secret

错误做法：

- `os.Getenv(...)` 直接写进插件

后果：

- secret 逻辑分散
- 无法统一审计
- 后续换 secret provider 成本极高

### 反例三：状态标记过度乐观

错误做法：

- 只完成 hotlist 就标记 `implemented`

后果：

- 审计误导
- 平台状态失真

### 反例四：TODO 写得太空泛

错误写法：

- `继续优化`
- `后续完善`

正确写法：

- `补 detail fetch fixture`
- `实现 auth 失败分类`
- `补 source sync metadata 测试`

---

## 16. 推荐验收清单

每个站点完成后建议自查：

- [ ] source 配置已补齐
- [ ] `config/config.json` 已配置必要参数
- [ ] 真实插件已替换 placeholder
- [ ] `FetchHotlist()` 已完成
- [ ] 如需 detail，`FetchArticle()` 已完成
- [ ] 标准化输出已完成
- [ ] resolver 接入已通过
- [ ] auth factory 接入已通过
- [ ] HTTP factory 接入已通过
- [ ] run / health 路径已通过
- [ ] fixture 已完成
- [ ] 回归测试已完成
- [ ] `status` 已更新

---

## 17. 一句话纪律

后续所有新站点开发，默认遵守以下三条：

1. 先进入统一体系，再补站点细节
2. 先写清配置和工作目标，再写插件代码
3. 不要把 secret、retry、timeout、auth 再硬编码回插件内部

只要持续按本文档推进，collector 就能够逐站点稳定扩展能力，而不是再产生新的孤立实现。
